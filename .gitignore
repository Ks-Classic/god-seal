# Python env / cache
.venv/
__pycache__/
*.pyc

# Source handbook PDFs: large (~350MB total) & copyrighted input — keep local only
*.pdf

# OS
.DS_Store
# reports: keep only chart HTML + interpretation.json + benchmarks
reports/*
!reports/kohata_yasuhiko_chart.html
!reports/kohata_yasuhiko_interpretation.json
!reports/oss_extraction_benchmark.json
